

- if position.user
  %li.statement{:class=>"bucket-#{position.stance_bucket}", :id=>"user-#{position.user_id}"}
    .view_statement
      - if position.user.avatar
        = image_tag position.user.avatar.url(:small), :alt => "#{h position.user.name}'s profile picture"
      
      .username
        = h position.user.name    

      .userid.hide
        #{position.user_id}


    // .full_position_details.hide
    //  = render :partial => "positions/statement/show", :locals => { :position => position }
  
