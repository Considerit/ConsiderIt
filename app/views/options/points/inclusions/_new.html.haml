- if @user
  .judgepointform{ :id => "judgepointform_#{point.id}" }
    - form_for :inclusion, :remote => true, :url => option_point_inclusions_path(@option, point), do |f|
      %input{:class => 'judge_point include no_qtip', :type => 'submit', :value => '', :name => "judge", :title => 'Click to add this point to your list. You can remove it later if you want.'}
          
:javascript
  $j("#judgepointform_#{point.id}").bind('ajax:success', function(data, status, xhr){
    callback_judge_point_success(status, #{point.id}, 1, #{is_from_other_list})
  });
  