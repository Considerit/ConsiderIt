/ = link_to 'Unsubscribe', unfollow_url(:host => @host, :u => @user.id, :i => @options[:current_tenant].id, :m => 'Account', :t => ApplicationController.token_for_action(@user.id, @options[:current_tenant], 'unfollow') )

/ 

- link = edit_notifications_url(@user.id, :host => @host, :u => @user.email, :t => ApplicationController.arbitrary_token("#{@user.email}#{@user.private_token}#{@options[:current_tenant].identifier}" )

- if text
  To unsubscribe#{description}: #{link}

- else
  = link_to 'Unsubscribe', link

  = description
