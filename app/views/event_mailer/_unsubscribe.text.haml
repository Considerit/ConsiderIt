- link = edit_notifications_url(@user.id, :host => @host, :u => @user.email, :t => ApplicationController.arbitrary_token("#{@user.email}#{@private_token}#{@options[:current_tenant].identifier}" ) )

To unsubscribe#{description}: #{link}