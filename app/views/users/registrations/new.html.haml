
#registration_form

  .primary
    %h2
      - if @context == 'position_submit'
        Sign in to save your position!
      - else
        Sign in

    #third_party

      %h3
        One-click sign in with your account from...

      - ['facebook', 'twitter', 'google', 'yahoo'].each do |provider|
        %a{ :onclick => "javascript:login('#{new_user_session_path(:third_party=>true, :provider=>"#{provider}")}',500, 500)" }
          = image_tag("#{provider}.png")

      .fineprint
        We won't post to your account or share your info without permission.

    .divider
      &mdash; or &mdash;

    #site_registration

      = form_for(:user, :as => :user, :url => user_registration_path, :html => { :multipart => true }) do |f|
      
        %ul
          %li
            = devise_error_messages!


          // %li.name_field
          //   = f.text_field :name, :class => 'has_example', :title => 'first and last name'

          //   %span.add_qtip{ :title => 'You stand behind your opinions, don’t you? When possible, we believe it is best to avoid anonymity.'}
          //     why?
            


          %li.email_field.inlined

            //{ :title => 'Your email is never shown to other users and we promise not to share your information with third parties. Your email is only used to confirm your account and to send notifications of relevant events, such as when someone replys to a comment you left.'}

            = f.label :email, 'Email address'
            = f.email_field :email, :title => 'email address', :required => true

          %li.password_field.inlined
            = f.label :password
            = f.password_field :password, :title => 'password', :required => true

          -# %li.avatar_field
            -#= f.file_field :avatar, :class => 'has_example', :title => 'profile picture'
        
          %li.submit_footer
            %input.proxy{ :type => 'button', :value => 'sign in' }

            = f.submit "sign in"

            %a.cancel
              x

    #acknowledgment
      Signing up acknowledges that you were bored by and agree to the 
      %a
        Terms of Use.

  .secondary
    %h3
      Why Sign In?

    %hr.ledge.top
    %hr.ledge.bottom
    
    %ul

      %li    
        Your vote will count in the totals.

      %li
        Share your points for other people to consider.
        
      %li
        Comment on the points others have added.  








