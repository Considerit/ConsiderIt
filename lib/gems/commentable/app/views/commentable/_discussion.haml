

.discuss    
  %h3.discussion_header    
    Discussion

  %ul.comment_section
    = render :partial => "commentable/comment", :collection => comments, :as => :comment
    
    - if enable_new_comments
      = render :partial => "commentable/new", :locals => { :commentable => commentable, :context => context, :comments => comments }