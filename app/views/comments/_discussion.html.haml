    
%ul.comment_section
  - comments.each do |comment|
    = render :partial => "comments/comment", :locals => { :comment => comment }

  %a{ :id=>'new_discussion', :name => 'new_discussion' }
  
  = render :partial => "comments/new_comment", :locals => { :point => point, :new_thread => true, :child => false, :parent_id => nil, :comments => comments }

