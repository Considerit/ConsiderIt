!!! Strict
%html(xmlns="http://www.w3.org/1999/xhtml")
  %head
    %meta{ :content => "text/html; charset=UTF-8", "http-equiv".intern => "Content-Type"}

    %style
      blockquote { padding-left: 10px; border-left: 1px solid #666; }
  %body
    %p
      Hi #{@user.name},
    
    %p
      #{@point.user.name} added a new #{@point.is_pro ? 'pro' : 'con'} point for "#{@point.proposal.title}":

    %blockquote
      #{@point.nutshell}

    %p 

      Check it out at
      = succeed ', ' do
        %a{ :href => "#{link_to proposal_point_url(@point.proposal.long_id, @point, :host => @host)}"}
      or visit the #{link_to 'proposal page', proposal_url(@point.proposal, :host => @host)}. 

    %p
      Thanks!

    %hr

    %p
      To unsubscribe to these messages, visit the link above, and modify your review by deselecting the checkbox near the bottom of the page.   