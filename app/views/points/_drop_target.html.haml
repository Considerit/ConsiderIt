.droppable{ :id => "drop-#{position == 1 ? 'pro' : 'con'}"}
  .bullet_point.hide
    -if position == 1
      +
    - else 
      &#x2013;
  
  .prompt
    %strong
      drag and drop     
    #{position == 1 ? 'pros' : 'cons'} you agree with.
  
  .clear