- link = edit_notifications_url(@user.id, :host => @host, :u => @user.email, :t => ApplicationController.arbitrary_token("#{@user.email}#{@user.unique_token}#{@options[:current_tenant].identifier}" ) )

= link_to 'Unsubscribe', link

= description
