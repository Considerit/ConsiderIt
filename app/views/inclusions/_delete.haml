.judgepointform{ :id => "judgepointform_#{point.id}" }
  = form_for :inclusion, :remote => true, :url => option_point_inclusion_path(@option, point, 1), :html => { :method => :delete } do |f|      
    %input{:class => 'judge_point dismiss no_qtip', :type => 'submit', :value => '', :name => "judge", :title => 'Click to remove this point from your list'}
          
:javascript
  $j("#judgepointform_#{point.id}").bind('ajax:success', function(data, status, xhr){
    ConsiderIt.inclusions.delete_callback(status, #{point.id})
  });
  