%script{ :type => "text/template", :id => "tpl_static_position" }

  .static-position-reasons.static-position-reasons-supporting
    (( if(supporting_points.length > 0){ ))
    .static-position-reasons-label
      ((if (stance != 3) { ))
      Their reasons include: 
      (( } else {  ))
      They recognize these Pros:
      (( } ))
    %ul.static-position-reasons-list          
      (( _.each(supporting_points, function(pnt){ ))

      %li.static-position-point
        {{ pnt.get('nutshell') }} 
        %span.username
          &ndash; {{ pnt.getUser().get('name') }}
      (( }); ))

    (( } else { ))
    .static-position-reasons-none
      {{ user.name }} does not give any supporting reasons.
    (( } ))
  .static-position-reasons.static-position-reasons-opposing
    (( if(opposing_points.length > 0){ ))
    .static-position-reasons-label
      At the same time, {{ user.name }} recognizes these tradeoffs:          
    %ul.static-position-reasons-list          
      (( _.each(opposing_points, function(pnt){ ))
      %li.static-position-point
        {{ pnt.get('nutshell') }} 
        %span.username
          &ndash; {{ pnt.getUser().get('name') }}
      (( }) ))

    (( } else { ))
    .static-position-reasons-none
      {{ user.name }} does not recognize any tradeoffs mitigating their position. 
    (( } ))